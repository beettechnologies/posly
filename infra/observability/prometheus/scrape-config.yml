global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: posly-server
    metrics_path: /metrics
    static_configs:
      - targets:
          - posly-server:8080
        labels:
          service: posly-server
          environment: dev
